# System /opt

Files under this directory deploy to **`/opt/`** on the target system.

- Repo `opt/` → `/opt/`
- `opt/cursor-example-repo/` → `/opt/cursor-example-repo/` (app code, scripts, app-local config).
